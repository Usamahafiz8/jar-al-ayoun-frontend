import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ED1B24', // Set your primary color
    },
    secondary: {
      main: '#ff6f61', // Set your secondary color
    },
  },
});

export default theme;
